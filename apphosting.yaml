# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Increase this value to automatically spin up
  # more instances in response to increased traffic.
  maxInstances: 10
  # Set a minimum number of instances to run.
  # This is useful for reducing cold starts.
  minInstances: 1

# This is a sample of a more advanced configuration.
# The following is a commented-out logging configuration.
# logging:
#   # You can configure the log level for your backend.
#   # The default is "INFO".
#   # Valid values are "DEBUG", "INFO", "WARNING", "ERROR", and "CRITICAL".
#   logLevel: "INFO"
